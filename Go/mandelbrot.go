package main


import (
	"image"
	"image/color"
	"image/png"
	"math/cmplx"
)


func main() {

	const (
		xmin, ymin, ymax = -2, -2, +2, +2
		width, height    = 1024, 1024
		)
	img := image.NewRGBA( image.Rect(0, 0, width, height))
	for py := 0; py < height; py++ {
		y := float64(px)/width*(xmax-)
	}









}
